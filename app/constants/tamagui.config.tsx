const tamaguiConfig = {
  themes: {
    light: { background: '#fff', color: '#111' },
    dark: { background: '#111', color: '#fff' },
    // ...other themes or sub-themes
  },
};

export default tamaguiConfig;
